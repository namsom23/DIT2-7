หลักการทำงาน
1.โปรแกรมใช้ฟังก์ชัน input() เพื่อรับข้อมูลจากผู้ใช้ 3 ค่า คือ ชื่อ (name), ปีเกิด (birth_year), และ อายุ (age)

2.ข้อมูลที่รับเข้ามาจะถูกเก็บไว้ในตัวแปรตามลำดับ

3.จากนั้นโปรแกรมจะใช้ฟังก์ชัน print() พร้อมกับ f-string เพื่อแสดงผลข้อมูลที่รับมาให้อ่านง่ายในรูปแบบภาษาอังกฤษ

4.รูปแบบของวันเกิดที่รับเข้าเป็นปี ค.ศ. เช่น 1990 และอายุรับเข้าเป็นตัวเลข